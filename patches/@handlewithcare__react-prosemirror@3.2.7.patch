diff --git a/dist/cjs/hooks/useNodeViewDescription.js b/dist/cjs/hooks/useNodeViewDescription.js
index ae04a6e..cd5d9ef 100644
--- a/dist/cjs/hooks/useNodeViewDescription.js
+++ b/dist/cjs/hooks/useNodeViewDescription.js
@@ -94,8 +94,17 @@ function useNodeViewDescription(getDOM, getContentDOM, constructor, props) {
         create,
         destroy
     ]);
+    // React detaches a replaced callback ref by calling it with null
+    // before attaching its successor, so a null DOM here is always
+    // transient: the attach pass that follows re-syncs the desc, and a
+    // real unmount destroys it in the layout effect cleanup. Tearing the
+    // desc down on the detach pass would destroy and recreate it (or, if
+    // no effect re-runs this render, lose it outright, #276) even though
+    // the DOM never changed.
+    const domDetached = (0, _useEffectEvent.useEffectEvent)(()=>getDOM() == null);
     const refUpdated = (0, _react.useCallback)(()=>{
         if (!viewDescRef.current) return;
+        if (domDetached()) return;
         if (!update()) {
             destroy();
             viewDescRef.current = create();
@@ -103,6 +112,7 @@ function useNodeViewDescription(getDOM, getContentDOM, constructor, props) {
     }, [
         create,
         destroy,
+        domDetached,
         update
     ]);
     (0, _useClientLayoutEffect.useClientLayoutEffect)(()=>{
diff --git a/dist/esm/hooks/useNodeViewDescription.js b/dist/esm/hooks/useNodeViewDescription.js
index 30224e4..6ff8ee6 100644
--- a/dist/esm/hooks/useNodeViewDescription.js
+++ b/dist/esm/hooks/useNodeViewDescription.js
@@ -84,8 +84,17 @@ export function useNodeViewDescription(getDOM, getContentDOM, constructor, props
         create,
         destroy
     ]);
+    // React detaches a replaced callback ref by calling it with null
+    // before attaching its successor, so a null DOM here is always
+    // transient: the attach pass that follows re-syncs the desc, and a
+    // real unmount destroys it in the layout effect cleanup. Tearing the
+    // desc down on the detach pass would destroy and recreate it (or, if
+    // no effect re-runs this render, lose it outright, #276) even though
+    // the DOM never changed.
+    const domDetached = useEffectEvent(()=>getDOM() == null);
     const refUpdated = useCallback(()=>{
         if (!viewDescRef.current) return;
+        if (domDetached()) return;
         if (!update()) {
             destroy();
             viewDescRef.current = create();
@@ -93,6 +102,7 @@ export function useNodeViewDescription(getDOM, getContentDOM, constructor, props
     }, [
         create,
         destroy,
+        domDetached,
         update
     ]);
     useClientLayoutEffect(()=>{
